``` mermaid
graph TD

    subgraph 配仓ID来源
        A1[业务触发<br/>calculateAndSaveWarehouseAllocationCost] 
        A1 --> A2[直接收集配仓ID]
        A2 --> A3[Redis<br/>warehouse_allocation:waybill_update]

        B1[业务触发<br/>订单成本变更]
        B1 --> B2[Redis<br/>order_cost_updates<br/>收集运单ID]
        B2 --> B3[processContainerShipAndDedupe]
        B3 --> B4[根据运单查询真实提单]
        B4 --> B5[提取配仓ID]
        B5 --> A3
    end

    subgraph 定时任务处理
        C1[定时任务<br/>handleWarehouseAllocationCostCalculation]
        C1 --> C2[读取 Redis<br/>warehouse_allocation:waybill_update]
        C1 --> C3[读取 Redis<br/>order_cost_updates]
        C2 --> C4[合并配仓ID]
        C3 --> C4
        C4 --> C5[配仓ID去重]
        C5 --> C6[开始处理配仓成本计算]
    end

    subgraph 收尾处理
        C6 --> D1[删除 Redis<br/>warehouse_allocation:waybill_update]
        C6 --> D2[删除 Redis<br/>order_cost_updates]
        D1 --> D3[定时任务完成]
        D2 --> D3
    end
```    