## mysql

docker run -d \
  --name mysql8 \
  -p 3306:3306 \
  -e MYSQL_ROOT_PASSWORD=12345687 \
  -e MYSQL_USER=applestven \
  -e MYSQL_PASSWORD=12345687 \
  -e MYSQL_DATABASE=app_db \
  mysql:8.0
EG

## dbgate
docker run -d \
  --name dbgate \
  -p 3000:3000 \
  -v dbgate-data:/root/.dbgate \
  dbgate/dbgate